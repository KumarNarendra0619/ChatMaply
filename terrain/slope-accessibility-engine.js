// BUILD-38: slope and accessibility calculations.
// Uses DEM elevations supplied by a terrain provider. No slope is fabricated.
const R=6371000;
const rad=d=>d*Math.PI/180;
export function calculateSlopeDegrees(e1,e2,horizontalDistanceM){const a=Number(e1),b=Number(e2),d=Number(horizontalDistanceM);if(!Number.isFinite(a)||!Number.isFinite(b)||!Number.isFinite(d)||d<=0)return null;return Math.atan(Math.abs(b-a)/d)*180/Math.PI;}
export function classifySlope(degrees){const s=Number(degrees);if(!Number.isFinite(s)||s<0)return'UNKNOWN';if(s<5)return'FLAT';if(s<15)return'GENTLE';if(s<30)return'MODERATE';if(s<45)return'STEEP';return'VERY_STEEP';}
export function assessAccessibility({slope_degrees,road_distance_m=null,surface='UNKNOWN'}={}){const slope=Number(slope_degrees);const road=Number(road_distance_m);if(!Number.isFinite(slope)||slope<0)return{class:'UNKNOWN',reason:'SLOPE_UNAVAILABLE'};if(slope>=45)return{class:'DIFFICULT',reason:'VERY_STEEP'};if(slope>=30)return{class:'LIMITED',reason:'STEEP'};if(Number.isFinite(road)&&road>500)return{class:'LIMITED',reason:'FAR_FROM_ROAD'};if(String(surface).toUpperCase()==='UNKNOWN')return{class:'TENTATIVE',reason:'SURFACE_UNKNOWN'};return{class:slope<15?'GOOD':'MODERATE',reason:'TERRAIN_SCREENING'};}
export function attachTerrainAssessment(observation={},terrain={}){const slope=terrain.slope_degrees??calculateSlopeDegrees(terrain.elevation1_m,terrain.elevation2_m,terrain.horizontal_distance_m);return{...observation,slope_degrees:Number.isFinite(Number(slope))?Number(slope):null,slope_class:classifySlope(slope),accessibility:assessAccessibility({slope_degrees:slope,road_distance_m:terrain.road_distance_m,surface:terrain.surface}),terrain_source:terrain.source??'UNKNOWN',terrain_status:slope===null?'UNKNOWN':'DERIVED'};}
