// BUILD-28 QA: only explicit, valid coordinates become map observations.
function coordinateOf(value={}){const latitude=Number(value.latitude??value.lat);const longitude=Number(value.longitude??value.lng);if(!Number.isFinite(latitude)||!Number.isFinite(longitude)||latitude<-90||latitude>90||longitude<-180||longitude>180)return null;return{latitude,longitude};}
function accuracyOf(value={}){const n=Number(value.accuracy_m??value.accuracy);return Number.isFinite(n)&&n>=0?n:null;}
function locationTypeOf(value={}){const raw=value.location_type??value.locationType;return raw?String(raw).toUpperCase():'UNKNOWN';}
function makeObservation(prefix,item,index,type,source,confidence){const coordinate=coordinateOf(item);if(!coordinate)return null;const accuracy_m=accuracyOf(item);return{id:`${prefix}-${item.id??index}`,type,latitude:coordinate.latitude,longitude:coordinate.longitude,lat:coordinate.latitude,lng:coordinate.longitude,accuracy_m,accuracy:accuracy_m,timestamp:item.timestamp||item.dateTime||item.date||item.time||null,source,confidence,location_type:locationTypeOf(item),evidence:[item]};}
export function buildObservations({messages=[],media=[]}={}){const observations=[];messages.forEach((m,i)=>{const o=makeObservation('msg',m,i,'Message report','message evidence','Reported');if(o)observations.push(o);});media.forEach((m,i)=>{const o=makeObservation('media',m,i,'Media observation',m.source||'media evidence',m.confidence||'Embedded');if(o)observations.push(o);});return observations;}
export function auditObservations(observations=[]){const checks=observations.map(o=>{const coordinate=coordinateOf(o),accuracy=accuracyOf(o);return{id:o?.id||null,coordinate_valid:Boolean(coordinate),accuracy_valid:accuracy===null||accuracy>=0,timestamp_present:Boolean(o?.timestamp),provenance_present:Boolean(o?.source),location_type_present:Boolean(o?.location_type),pass:Boolean(coordinate&&o?.source)};});return{total:checks.length,passed:checks.filter(c=>c.pass).length,failed:checks.filter(c=>!c.pass).length,checks};}
